import { Config } from '@stencil/core';
import { sass } from '@stencil/sass';

export const config: Config = {
  namespace: '<%= templateConfig.name %>',
  globalStyle: 'globals/app.scss',
  outputTargets: [
    {
      type: 'dist',
      esmLoaderPath: '../loader'
    },
    {
      type: 'docs-json',
      file: 'docs/components.json'
    }
  ],
  srcDir: './src',
  plugins: [
    sass(),
  ],
  testing: {
    testRegex: '/src/.*\\.unit.spec\\.(ts|tsx|js)$'
  }
};
