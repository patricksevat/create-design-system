import { Component, h } from '@stencil/core';

@Component({
	shadow: true,
	styleUrl: './button.scss',
	tag: '<%= `${templateConfig.prefix}-button` %>',
})
export class <%= `${templateConfig.prefixPascalCase}Button` %> {
	protected render() {
		return (
			<button
				class='<%= `${templateConfig.prefix}-button` %>'
			>
				My Button
			</button>
		);
	}
}
